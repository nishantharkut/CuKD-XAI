1335
Page 10 of 15
Signal, Image and Video Processing (2025) 19 :1335
Fig. 3 Heatmap illustrating the classiﬁcation performance of models across different classes
and thus generates a larger number of false positives. The pre-
cision of both DCQI and DCOA can be raised to a high level
(0.4338 and 0.3877, respectively), with DCQI providing the
highest F1-score (0.5969) as a result. Additionally, not only
do the hybrid ensembles in the “TDMA” and “Grayhole”
conditions contribute to a much higher precision, but also
the F1-scores over DNCA, which is the key to the role that
metaheuristic optimization plays in redeﬁning the decision
boundary. DCQI performance is also clearly observed in the
“Blackhole” case, where packet-drop actions were the main
features (precision: 0.6993, F1-score: 0.8060), while DCOA
can only achieve a limited improvement over the baseline.
In general, DCQI is more likely than DCOA to attain higher
precision and balanced classiﬁcation metrics, suggesting that
its optimization technique is more successful in locating the
decision boundaries that are compatible with the different
intrusion patterns in wireless sensor networks.
Figure 5 shows the Receiver Operating Characteristic
(ROC) curves along with the corresponding Area Under the
Curve (AUC) values for the baseline ensemble (DNCA) and
the metaheuristically enhanced models (DCQI and DCOA).
123
