Published in Transactions on Machine Learning Research (06/2024)
Feature Agreement:
ML practitioners in interviews (Section 3.1) indicated that a key notion of
disagreement between a pair of explanations is that they output different top-k features. To capture this
notion, we introduce the feature agreement metric which computes the fraction of common features between
the sets of top-k features of two explanations. Given two explanations Ea and Eb, feature agreement is
formulated as:
FeatureAgreementpEa, Eb, kq “ |TFpEa, kq X TFpEb, kq|
k
where TFpE, kq returns the set of top-k features of explanation E based on the magnitude of the feature
importance values. If the sets of top-k features of explanations Ea and Eb contain the same features, then
FeatureAgreementpEa, Eb, kq “ 1.
Rank Agreement:
Practitioners also indicated that if the ordering of the top-k features is different for
two explanations (even if the feature sets are the same), then they consider it to be a disagreement. To
capture this notion, we introduce the rank agreement metric which computes the fraction of features that are
not only common between the sets of top-k features of two explanations but also have the same position in
the respective rank orders. Rank agreement is a stricter metric than feature agreement since it also considers
the ordering of the top-k features. Given two explanations Ea and Eb, rank agreement is formulated as:
RankAgreementpEa, Eb, kq “
| Ť
fPF
tf | f P TFpEa, kq ^ f P TFpEb, kq ^ RpEa, fq “ RpEb, fqu|
k
where F is the complete set of features in the data, TFpE, kq is as defined above, and RpE, fq returns the
rank of feature f according to explanation E. If the rank-ordered lists of top-k features of explanations Ea
and Eb match, then RankAgreementpEa, Eb, kq “ 1.
Sign Agreement:
In our study, practitioners also mentioned that they consider two explanations to
disagree if the feature attribution signs (i.e. directions of feature contribution) do not align for the top-k
features. To capture this notion, we introduce the sign agreement metric which computes the fraction of
features that are not only common between the sets of top-k features of two explanations but also share
the same sign in both explanations. Sign agreement is a stricter metric than feature agreement since it also
considers signs of the top-k features. Given two explanations Ea and Eb, sign agreement is formulated as:
SignAgreementpEa, Eb, kq “
| Ť
fPF
tf | f P TFpEa, kq ^ f P TFpEb, kq ^ SpEa, fq “ SpEb, fqu|
k
where F and TFpE, kq are as defined above and SpE, fq returns the sign of feature f according to explanation
E.
Signed Rank Agreement:
This metric fuses the above three notions of explanation disagreement
and computes the fraction of features that are not only common between the sets of top-k features of two
explanations but also share the same feature attribution sign and rank in both explanations. The signed rank
agreement is the strictest compared to all the aforementioned metrics since it considers both the ordering and
the signs of the top-k features. Given two explanations Ea and Eb, signed rank agreement is formulated as:
SignedRankAgreementpEa, Eb, kq “
| Ť
fPF
tf | f P TFpEa, kq ^ f P TFpEb, kq ^ SpEa, fq “ SpEb, fq ^ RpEa, fq “ RpEb, fqu|
k
where F, TFpE, kq, SpE, fq, and RpE, fq are as defined above. SignedRankAgreementpEa, Eb, kq “ 1 if the top-k
features of two explanations match on all aspects (i.e., features, feature attribution signs, rank ordering) barring the
exact feature importance values.
19
