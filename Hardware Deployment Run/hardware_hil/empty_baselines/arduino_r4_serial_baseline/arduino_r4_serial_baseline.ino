// Arduino R4 serial baseline for CuKD-XAI HIL compile-overhead accounting.
// This contains no model, preprocessing, protocol parser, Wi-Fi, filesystem,
// or cloud path. Use the same Arduino R4 board package/settings as the
// Student A/B HIL sketches.

void setup() {
  Serial.begin(115200);
}

void loop() {
}

