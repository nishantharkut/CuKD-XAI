# CuKD-XAI notebook dependencies
# Target Python: 3.10 or 3.11
# For GPU support (NVIDIA CUDA 12.1), install torch separately from:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

numpy>=1.24,<2.0
pandas>=2.0
scikit-learn>=1.3,<1.6
scipy>=1.11
matplotlib>=3.7
seaborn>=0.12
shap>=0.44,<0.46
imbalanced-learn>=0.11

# Torch: install separately if you want CUDA. The line below is CPU-only.
# For CUDA, comment this out and use the index-url command above instead.
torch>=2.0,<2.5

# Optional: Jupyter for interactive running
jupyter>=1.0
ipykernel>=6.25
